@model List<DTOLayer.CauHoiDTO>

@if (Model != null)
{
    for (int i = (Model.Count - 1); i >= 0; i--)
    {
        @Html.Partial("~/Views/HoiDap/_Item_DanhSachCauHoi.cshtml", Model[i])
    }
}