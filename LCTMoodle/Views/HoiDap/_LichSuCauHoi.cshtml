@using DTOLayer;
@using LCTMoodle.LCTView;
@model List<HoatDongDTO>

<section class="lich-su-cau-hoi">
    
        @if (Model != null)
        {
            <ul>
                @foreach (HoatDongDTO hoatDong in Model)
                {
                <li>
                    @HoatDongView.link(hoatDong)
                </li>
                }
            </ul>

        }
        else
        {
            <p class="empty">Chưa có hoạt động</p>
        }
</section>